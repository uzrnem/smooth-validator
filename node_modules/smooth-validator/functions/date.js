const dataType = require('../dataType');

module.exports = {
  validateDate: function (varName, varValue) {
    if (!dataType.isDate(varValue))
      return { resKey: varName, resMsg: ' is not in date format' };
    return { resKey: false, resMsg: null };
  },
}