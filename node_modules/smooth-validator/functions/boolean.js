const dataType = require('../dataType');

module.exports = {
  validateBoolean: function (varName, varValue) {
    if (!dataType.isBoolean(varValue))
      return { resKey: varName, resMsg: ' is not boolean' };
    return { resKey: false, resMsg: null };
  },
}